name: Caliper Benchmarks
version: "2.0.0"

caliper:
  blockchain: fabric
  sutOptions:
    mutualTls: true

channels:
  # channelName of mychannel matches the name of the channel created by test network
  - channelName: mychannel
    # the chaincodeIDs of all the fabric chaincodes in caliper-benchmarks
    contracts:
    - id: fabcar
    - id: fixed-asset
    - id: marbles
    - id: simple
    - id: smallbank
    - id: event_transfer_iot

organizations:
  - mspid: Org1MSP
    # Identities come from cryptogen created material for test-network
    identities:
      certificates:
      - name: 'User1'
      # I've added full path to the in "keystore"folder;
      # File name for the "clientPrivateKey" which I found in the folder "keystore" must be always updated whenever you start new "fabric-test-network"; 
      # Also, for the "clientSignCerts" part, I've adjusted the name of the file as it is in the folder, and has a name "cert.pem".
        clientPrivateKey:
          path: '/home/vodenica/go/src/github.com/hyperledger/fabric-samples/test-network/organizations/peerOrganizations/org1.example.com/users/User1@org1.example.com/msp/keystore/d5d6e134aa6a1515a443278bce6c20e6f0b9f2f1fd70dcd0347b8d135777c2b0_sk'
        clientSignedCert:
          path: '../fabric-samples/test-network/organizations/peerOrganizations/org1.example.com/users/User1@org1.example.com/msp/signcerts/cert.pem'
    connectionProfile:
      path: '../fabric-samples/test-network/organizations/peerOrganizations/org1.example.com/connection-org1.yaml'
      discover: true