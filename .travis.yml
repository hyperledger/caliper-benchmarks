language: python
cache: pip
python:
  - "3.6"

install:
  - pip install mkdocs
  - pip install mkdocs-material
  - pip install pymdown-extensions

script:
  - mkdocs build

deploy:
  provider: script
  script: mkdocs gh-deploy --force
  on:
    branch: gh-pages